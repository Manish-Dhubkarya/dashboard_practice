import { makeStyles } from "@mui/styles";
export const useStyleDisplayProduct = makeStyles({
    div1: {
        width: "60%", borderRadius: "10px", height: "auto", padding: 10, background: "#f6ff5d"
    },
})